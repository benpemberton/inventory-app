extends ../layout

block content
  h1= title
  p Here is a list of all the products that we are currently stocking.

  if products.length

    ul
    each product in products
      li
        a(href=product.url) #{product.name}

  else 

   h4 No products could be found!
